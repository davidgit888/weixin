<!--pages/image/image.wxml-->
<text>请保证相片清晰</text>
<text>{{testdata}}</text>
  <view class="page-body">
    <view class="page-body">
    <camera device-position="back" flash="off" binderror="error" style="width: 100%; height: 300px;"></camera>
      <!-- <view class="btn-area">
      <button type="primary" bindtap="takePhoto">拍照</button>
      </view> -->
      <!-- <view class="preview-tips" wx:if="{{useBtn}}">预览
      </view> -->
      <!-- <image wx:if="{{src}}" mode="widthFix" src="{{src}}" style="width: 100%; height: 300px;"></image> -->
      </view>
      <view class="btn-area">
        <button type="primary" bindtap="sendPhoto" wx:if="{{useBtn}}">上传照片</button>
      </view>
  </view>
